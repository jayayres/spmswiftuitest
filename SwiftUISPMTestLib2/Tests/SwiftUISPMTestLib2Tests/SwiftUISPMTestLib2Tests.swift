import XCTest
@testable import SwiftUISPMTestLib2

final class SwiftUISPMTestLib2Tests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUISPMTestLib2().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
